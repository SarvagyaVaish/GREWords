<%= form_for(@whats_new_message) do |f| %>
  <% if @whats_new_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@whats_new_message.errors.count, "error") %> prohibited this whats_new_message from being saved:</h2>

      <ul>
      <% @whats_new_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_field :message %>
  </div>
  <div class="field">
    <%= f.label :status %><br />
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
