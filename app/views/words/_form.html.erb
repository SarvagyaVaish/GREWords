<%= form_for @word, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :meaning, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :meaning, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :sentence, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :sentence, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                words_path, :class => 'btn' %>
  </div>
<% end %>
